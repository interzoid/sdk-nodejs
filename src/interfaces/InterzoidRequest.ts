/**
 * Base interface for all Interzoid requests
 * @interface InterzoidRequest
 * @property {string} apiKey - The API key to use for the request
 */
export interface InterzoidRequest {
  apiKey: string;
}
