/**
 * Interface for Interzoid API response
 * @interface InterzoidResponse
 * @property {string} code - The response code
 * @property {string} credits - The number of credits remaining
 */
export interface InterzoidResponse {
  code: string;
  credits: string;
}
